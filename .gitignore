__pycache__
src/__pycache__/
src/cache/__pycache__/
src/tools/__pycache__/
src/core/__pycache__/
src/rag/__pycache__/
src/pipeline/__pycache__/
src/configs/__pycache__/
src/search/__pycache__/
scripts/__pycache__/
/tests/__pycache__/
.DS_Store
.gradio/
.pytest_cache/
.ruff_cache/
*.egg-info/
.venv/
data/rag_data/
# ---- SQLite 运行时临时文件（diskcache 用 WAL 模式）----
# `data/qa_cache/` 与 `data/search_cache/` 里的 cache.db 是**被追踪**的
# （仓库自带一批预热问答，让新克隆的人开箱就有 L1 命中）。但 SQLite 的
# WAL 模式会额外产生两个**纯运行时**文件：
#     cache.db-wal  预写日志（本次实测涨到 4MB，且每次读写都在变）
#     cache.db-shm  共享内存索引（进程间协调用，内容与机器状态绑定）
# 这两个文件不能进版本库，原因：
#   ① 它们每跑一次程序就变，会让 git status 永远是脏的、diff 噪声极大；
#   ② 跨机器/跨进程无意义，且与 cache.db 存在版本耦合 —— 一旦
#      WAL 与主库不匹配，SQLite 可能拒绝打开或读到不一致状态。
# 正确做法：提交前对主库做 `PRAGMA wal_checkpoint(TRUNCATE)`，
# 把 WAL 里的数据落进 cache.db，再只提交 cache.db。
*.db-wal
*.db-shm

# ⚠️ 「cache.db 被 git 追踪」这个决定有一个必须知道的副作用：
# **任何切换工作区内容的 git 操作都会按字节覆写 cache.db，从而损坏它。**
# 包括 `git stash` / `git stash pop` / `git checkout <branch>` / `git reset --hard`。
# 症状是下次启动直接抛：
#     sqlite3.DatabaseError: database disk image is malformed
# 因为 git 只管文件内容一致，不理解 SQLite 的页结构与 rowid 顺序，
# 覆写后主库与残留的 WAL/SHM 也不再匹配。
#
# 实测损坏形态（4 个库全中）：
#     Tree 3 page 3 cell 4: Rowid 3017 out of order
#     row 6 missing from index sqlite_autoindex_Settings_1
# 注意数据页本身通常是好的 —— 记录都还读得出来，坏的是索引和 rowid 顺序。
#
# 因此**不要用 `git checkout` 覆盖来"修"它**（git 里的版本可能比工作区旧，
# 实测差 1 条），应该用 `sqlite3 <db> .recover` 重建：
#     sqlite3 cache.db ".recover" > rec.sql
#     sqlite3 rec.db < rec.sql && mv rec.db cache.db && rm -f cache.db-wal cache.db-shm
# `.recover` 直接扫描数据页抽取记录，绕过损坏的索引结构；
# 而 `.dump` 走正常 B-tree 遍历，遇到 rowid 乱序会直接报错中断。
#
# 若要做 git 分支/stash 操作，最省事的做法是先把这 4 个库排除在外：
#     git stash push -- <只列出你真正要暂存的文件>

# ---- diskcache 外溢文件（兜底忽略）----
# diskcache 对超过 disk_min_file_size 的单条 value 不再内联进 cache.db，
# 而是外溢成独立文件：<两位hex>/<两位hex>/<28位hex>.val（目录名纯随机，
# 与 query 无关，仅为避免单目录堆积过多文件）。
#
# 项目已在 config.SEARCH_CACHE_MIN_FILE_SIZE 把阈值从默认 32KB 抬到 1MB，
# 正常情况下不会再产生这类文件（实测搜索结果 avg≈11KB / max≈26KB）。
# 这里仍保留忽略规则作为兜底，理由：cache.db 是被追踪的，而外溢目录名随机、
# 无法预先写进追踪列表 —— 若只提交 cache.db，其中的 filename 指针在别人机器上
# 指向不存在的文件，命中该 key 不是缓存 miss 而是读文件抛异常。
# 与其留下这种不一致，不如让外溢文件始终不入库（最坏结果只是缓存少命中一条）。
data/qa_cache/[0-9a-f][0-9a-f]/
data/search_cache/[0-9a-f][0-9a-f]/
*.val

# 评测数据集（体积大 + 有 canary 泄漏风险，不入库）
evals/__pycache__/
evals/data/
evals/out/
